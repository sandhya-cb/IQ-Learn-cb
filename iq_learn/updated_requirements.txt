# Python 3.9+ is highly recommended for PyTorch 2.x and SB3 2.x
#
# --- Core Deep Learning (PyTorch) ---
# NOTE: Check PyTorch website for the exact command for your CUDA version.
# These lines assume a CPU-only installation for compatibility:
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# --- Reinforcement Learning ---
stable-baselines3>=2.2.1
# Core environment package
gymnasium>=0.29.1,<1.0.0
# Specific environment dependencies
ale-py>=0.8.1
# box2d-py>=2.3.10  # Optional, usually included via gymnasium[box2d]

# --- Logging and Utilities ---
tensorboard>=2.16.0
wandb
tqdm>=4.66.1

# --- Other Requirements ---
hydra-core>=1.3.2
opencv-python>=4.10.0
tornado
trackio
tensorboardX
grad-cam